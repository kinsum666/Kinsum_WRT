# ========================================
# OEC (Rockchip RK3566) 固件配置
# 基于 ImmortalWrt
# ========================================

# ---------- Target System ----------
# 目标平台：Rockchip RK356x
CONFIG_TARGET_rockchip=y
CONFIG_TARGET_rockchip_rk3568=y
# 注意：如果 OEC 在列表中直接有对应选项，请选择它
# 如果没有，可以选择相近的设备，或在下方通过 DTS 指定
# CONFIG_TARGET_rockchip_rk3568_DEVICE_oec-rk3566=y  # 如果存在
# 或者使用通用配置：
CONFIG_TARGET_rockchip_rk3568_DEVICE_generic=y

# ---------- Kernel Modules ----------
# 基础内核模块（保留原配置，OEC 同样需要）
CONFIG_PACKAGE_kmod-tls=y
CONFIG_PACKAGE_kmod-tun=y

# Rockchip 特定驱动（OEC 需要额外添加）
CONFIG_PACKAGE_kmod-r8169=y          # 有线网卡（若使用 RTL8169）
CONFIG_PACKAGE_kmod-rtl88x2bu=y      # 无线网卡（若使用 RTL88x2BU）
CONFIG_PACKAGE_kmod-ata-ahci-platform=y  # SATA 支持
CONFIG_PACKAGE_kmod-usb-storage=y    # USB 存储支持
CONFIG_PACKAGE_kmod-usb3=y           # USB 3.0 支持
CONFIG_PACKAGE_kmod-sdhci=y          # SD 卡支持
CONFIG_PACKAGE_kmod-i2c-core=y       # I2C 支持
CONFIG_PACKAGE_kmod-spi-dev=y        # SPI 支持
CONFIG_PACKAGE_kmod-gpio-button-hotplug=y  # GPIO 按键支持

# ---------- Filesystem Support ----------
# 文件系统支持（OEC 通常需要 ext4 和 f2fs）
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-f2fs=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-ntfs=y

# ---------- Base Packages ----------
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_fuse-utils=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_sgdisk=y
CONFIG_PACKAGE_openssl-util=y
CONFIG_PACKAGE_qrencode=y

# ---------- Luci Apps (保持不变) ----------
CONFIG_PACKAGE_luci-app-adguardhome=y
CONFIG_PACKAGE_luci-app-adguardhome_INCLUDE_binary=n
CONFIG_PACKAGE_luci-app-autoreboot=y
CONFIG_PACKAGE_luci-app-dockerman=y
CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_luci-app-vlmcsd=y

# ---------- Additional Luci Apps (推荐添加) ----------
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-app-statistics=y

# ---------- Network Packages ----------
CONFIG_PACKAGE_iperf3=y              # 网络测速
CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_tc=y                  # 流量控制
CONFIG_PACKAGE_ebtables=y

# ---------- Utilities ----------
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget=y
CONFIG_PACKAGE_nano=y
CONFIG_PACKAGE_tree=y
CONFIG_PACKAGE_unzip=y
CONFIG_PACKAGE_gzip=y

# ---------- Docker Support (OEC 推荐) ----------
# Docker 需要额外内核模块支持
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_kmod-br-netfilter=y
CONFIG_PACKAGE_kmod-ikconfig=y
CONFIG_PACKAGE_kmod-nf-nat=y
CONFIG_PACKAGE_kmod-nf-conntrack=y
CONFIG_PACKAGE_docker-ce=y
CONFIG_PACKAGE_dockerd=y

# ---------- UEFI/GPT Support ----------
CONFIG_PACKAGE_partx-utils=y
CONFIG_PACKAGE_gptfdisk=y
CONFIG_PACKAGE_fdisk=y

# ---------- Bootloader/Uboot ----------
# 确保 OEC 能正常引导
# 如果是从 SD 卡启动，可能需要 uboot-rockchip
# CONFIG_PACKAGE_uboot-rockchip=y
